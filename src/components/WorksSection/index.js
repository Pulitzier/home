import React from "react";
import AboutSection from "../App/App";

let WorkSection = (props) => {
	return (
		<section id="works">
			<p>Тут наши работы</p>
		</section>
	)
};

export default WorkSection;